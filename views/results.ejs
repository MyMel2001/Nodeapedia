<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Searching - Nodeapedia</title>
    <link href="/css/style.css" rel="stylesheet">
    <script src="/js/theme.js" defer></script>
</head>
<body class="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 transition-colors duration-200">
    <header class="p-4 border-b dark:border-gray-700">
        <h1 class="text-2xl font-bold"><a href="/">Nodeapedia</a></h1>
    </header>
    <main class="max-w-4xl mx-auto p-4 mt-8">
        <h2 class="text-2xl font-bold mb-4">Results for "<%= query %>"</h2>
        <div class="space-y-4">
            <% results.forEach(result => { %>
                <div class="p-4 border dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800">
                    <h3 class="text-xl font-bold mb-1">
                        <a href="/article/<%= encodeURIComponent(result.title) %>" class="text-blue-600 dark:text-blue-400 hover:underline">
                            <%= result.title %>
                        </a>
                    </h3>
                    <p class="text-gray-600 dark:text-gray-400"><%- result.snippet %></p>
                </div>
            <% }) %>
        </div>
    </main>
</body>
</html>
